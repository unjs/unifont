/** The selection every catalogue figure is quoted against, so two families compare. */
export const BASELINE = {
  weights: ['400', '700'],
  styles: ['normal'],
  subsets: ['latin'],
  formats: ['woff2'],
} as const
